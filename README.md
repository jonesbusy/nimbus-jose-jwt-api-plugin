# Jenkins Nimbus JOSE JWT API Plugin

## Introduction

This plugin provides the Nimbus JOSE JWT API for other plugins.

# Build

To build the plugin locally:

```
mvn -Dset.changelist clean install
```
